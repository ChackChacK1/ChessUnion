spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url:
    username:
    password:
  application:
    name: ChessUnion
  flyway:
    baseline-on-migrate: true
    enabled: true
    user:
    password:
jwt:
  lifetime: 30m
  secret:
server:
  port: 8080